# Database connection - this should be set in Vercel environment variables
# For production, add DATABASE_URL in Vercel dashboard under Environment Variables
# DATABASE_URL=postgresql://username:password@hostname:port/database?sslmode=require